# DemoRestService Demonstration REST service.  Returns a "hello world" type greeting, with an automatically incremented ID/counter.  Strings submitted with "?name=<name>" will return "Hello <name>"
  
